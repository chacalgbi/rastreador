<tr>
  <th><%= event.driver_name %></th>
  <td><%= event.created_at.in_time_zone('America/Sao_Paulo').strftime('%d/%m/%Y %H:%M') %></td>
  <td class="text-end"><%= simple_format(auto_link(sanitize(event.message, tags: %w(br)), html: { target: '_blank' })) %></td>
</tr>